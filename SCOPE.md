# NbgAiHub — Scope

**Last updated:** 2026-05-25 (Late afternoon session: reader-mode for long-scroll content pages shipped — new `mode="reader"` prop on `<MarketingShell>` emits `data-mode="reader"` driving five quiet-rhythm CSS rules in `site/src/styles/agentnews-layout.css` (drop per-section borders, single mono eyebrow per step, one serif step-title weight, hairline-aside resources, single `--reader-step-gap` rhythm). New `.hero--stack` modifier for the stacked single-column hero. Applied to `/start-here/foundations/` + `/start-here/day-1/`. Glossary auto-linking extended to plain-text JSX strings via new `site/src/lib/glossary-link-string.ts` helper — shares the plugin's `getGlossaryIndex()` so single-source-of-truth, emits identical `<button data-glossary-slug>` HTML, wired into skills+tips+index card summaries, contribute cards, Foundations+Day 1 hero ledes. 0 → 7-11 auto-links per listing page. MyPins page rebuilt as a unified card grid with filter chips replacing the five-section type stack. Glossary `/glossary/` filter+A-Z rail now actually sticky (was theoretically sticky but inside a short parent) + `scroll-margin-top: 13rem` so hash-anchored terms land below the rail not behind it. Nested glossary tooltips ("hover inside hover") via build-time pre-linked `tldrHtml` + lazy nested `wire(pop)` on first show (previous eager-recursive approach generated hundreds of popovers + scroll listeners, locked the page — replaced). 2 new glossary terms authored: HTTP, API. Glossary count 28 → 34 (parallel session added 4 more). Day 1 page cleaned: pullquote removed, Week 1 references + page deleted, "Tomorrow / Three paths" → "Where next" with Glossary replacing the Week 1 card. `site/astro.config.mjs#base` removed for local dev so `localhost:4321/` serves at bare root with internal links working; env-driven re-add needed in deploy workflow before next Pages publish — see Issues #18. Two Starlight-cascade bugs surfaced + fixed via Puppeteer computed-style inspection (chip-row phantom 16px margin, scoped-CSS not hitting JS-created elements). Authoring doc `docs/reference/authoring-glossary-terms.md` updated with the "Two paths, one index" section. See DECISIONS 2026-05-25 reader-mode entry, Issues #17/#18.) Prior 2026-05-25 (Glossary auto-link + hover tooltips shipped. Schema extended in `site/src/content.config.ts` with `tldr: z.string().min(1).max(160)` (required, plain text, no fallback) and `aliases: z.array(z.string()).default([])`. All 21 existing glossary entries backfilled with tldr + aliases per the locked alias contract (pull-request→PR/PRs/pull request, repository→repo/repos, hook→hooks, skill→skills, plugin→plugins, large-language-model→LLM/LLMs, claudemd→CLAUDE.md, claude-code→Claude Code, context-window→context window/context windows, etc.). 7 new glossary entries authored: `cli`, `frontmatter`, `yaml`, `markdown`, `rss`, `model`, **`hook`** (added mid-flight when the planner caught that the alias contract referenced a missing entry). Glossary total: 21 → 28. New remark plugin at `site/src/plugins/remark-glossary-link.ts` (465 lines, single-layer unified Plugin) walks each markdown AST at build time, matches glossary slugs + aliases case-insensitively with explicit non-alphanumeric-lookaround word boundaries (NOT `\b` — handles hyphenated terms correctly), wraps the first match per canonical-slug-per-file with a plain HTML `<button data-glossary-slug="…" data-glossary-display="…">…source-cased text…</button>` in a `type: 'html'` mdast node. Skip rules: fenced code, inline code, headings h1-h6, existing links, Starlight asides (`:::tip` / `:::note` / `:::caution` / `:::danger`), the term's own glossary page, and any file path under `/news/published/` (per resolved OQ1 — news is hard-redirected externally per the prior nav rework). New primitive at `site/src/components/primitives/GlossaryTerm.astro` (≤270 lines, AC36/AC37 portability — zero `@astrojs/starlight` imports) injects a JSON manifest `{ [slug]: { title, tldr } }` once per page plus a small client-side wiring script (≤80 LOC, no framework deps) that, on `DOMContentLoaded` + `astro:page-load` (view-transitions safety), finds every `[data-glossary-slug]:not([data-nbg-glossary-wired])`, generates a unique id (`gloss-<crypto.randomUUID().slice(0,8)>`), sets `popovertarget` + `aria-describedby` + `type="button"`, creates a sibling `<span popover="auto" role="tooltip" class="nbg-glossary-popover">` containing the term title (h?), `tldr`, and "Read more →" anchor to `/glossary/#<slug>`, then wires hover (80ms debounce open) + focus + click via JS `.showPopover()` / `.hidePopover()`. ESC dismissal is native to the HTML `popover` attribute. Surface uses only `--nbg-*` semantic tokens (per §S.14.8 — surface bg, border, shadow lg, radius md/xs, fg-primary/secondary, accent, link). Reduced-motion: `prefers-reduced-motion: reduce` zeroes transitions on trigger + popover. Wired in `site/astro.config.mjs` via `markdown.remarkPlugins`; ALSO passed explicitly into the manual `createMarkdownProcessor()` calls in `site/src/pages/start-here/foundations.astro` and `site/src/pages/start-here/day-1.astro` because those bypass the project markdown config (see Issue #15). MarketingShell renders one `<GlossaryTerm />` instance per page. Build-output evidence: `foundations/index.html` carries 33 buttons, `day-1/index.html` carries 10 buttons. Audit script at `scripts/audit-glossary-candidates.mjs` (stdlib-only ESM Node) scans 94 corpus files via 3 detectors (acronyms regex `\b[A-Z]{2,5}s?\b` count≥3, backticked terms count≥3, frequent nouns count≥5) and writes `docs/reference/glossary-audit-2026-05-25.md` (14 candidate acronyms — `AI/API/CI/GUI/HTML/IDE/JIRA/JS/JSON/NBG/UI/URL/VS/WSL` — 3 backticked, 500 noisy nouns for human triage); guards against any writes outside `docs/reference/glossary-audit-*`. **One subtle bug found and fixed mid-flight:** the plugin was initially authored double-wrapped (factory returned a Plugin function that returned a Transformer instead of returning the Transformer directly). Unified's `.use(plugin, options)` calls `plugin(options)` and expects the return to be a Transformer; the double-wrap made unified treat the inner Plugin as the transformer and call it with `(tree, file)` — which the no-args inner function ignored, returning yet another transformer that got discarded. Bodies came back empty until the factory was flattened to single-layer `Plugin<[Options], Root, Root>`. Found via build-output diff (foundations page step bodies rendering as empty `<div>`) after the smoke-test-in-isolation passed. **Issue #17 closed in-flight:** `/glossary/` catalog page initially didn't auto-link (Astro 6's content-collection `render(entry)` path bypasses project-level `markdown.remarkPlugins`). Fixed in the same session by rewriting `glossary.astro` to use `createMarkdownProcessor({ remarkPlugins: [[remarkGlossaryLink, …]] })` per entry, with a synthesised `fileURL` so the self-page skip still works (an entry doesn't link its own term in its own body). Result: 74 buttons on `/glossary/` (with self-skip verified — `agent` entry links `model`/`claude-code`/`context-window`/`prompt`/`token` but not "agent"). 33 buttons on foundations + 10 on day-1 unchanged. All three surfaces auto-link correctly. See DECISIONS 2026-05-25 glossary-tooltips entry, `docs/refined-requests/glossary-tooltips.md`, `docs/design/plan-006-glossary-tooltips.md`, `docs/design/project-design.md` §S.14.) Prior 2026-05-25 (Navigation rework + two-door landing shipped. Sidebar flattened to one entry per pillar — Home · Foundations · Day 1 · Skills · Tips & Tricks · Glossary · News ↗ · My Pins (was 13 entries across 3 groups). `Reference` page deleted entirely (14 entries were either redundant with Tips/Glossary or `status: "planned"` placeholders). `Contribute` group + `Submit a Skill` unlinked from nav (page files retained as orphan routes; reversible). News surface now hard-redirects to a colleague's `https://biks2013.github.io/AgentNews/` via `redirects` in `astro.config.mjs`; `site/src/pages/news/` directory deleted; all News links use `target="_blank"`. Branding rule: "News" everywhere in the UI, never "AgentNews". Landing page (`index.astro`) fully rewritten as a two-door traffic router above the fold — Newcomer card (teal-soft, two buttons: Open Foundations + Day 1 setup) / Experienced card (white, 4-pill row: Skills · Tips & Tricks · Glossary · News ↗). News feed section removed from the homepage (destination moved). Headline kept as "What I wish I knew sooner about Claude Code"; copy rewritten to mention LLM-provider differentiation, why Claude Code, and setup (no more "X explainers + Y videos" counts). 237 → 232 site tests passing (4 tests for the deleted routes removed). One hard-won discovery: Starlight's `.sl-markdown-content` wrapper ships a global vertical-rhythm rule (`:not(a, ...) + :not(a, ...) { margin-top: 1rem }`) that silently added 16px margin-top to the second router card, offsetting it down — surfaced after eight blind CSS iterations via Puppeteer + Chrome DevTools Protocol `CSS.getMatchedStylesForNode`. Override: `.router-grid > * + * { margin-top: 0 }`. Same incident triggered a new top-level rule in global `~/.claude/CLAUDE.md` — "Visual verification for UI work — don't iterate blind" — that mandates a headless-Chrome / Puppeteer / CDP escalation after one failed iteration on framework cascades. See DECISIONS 2026-05-25, Issues #14 / #15.) Prior 2026-05-24 (AgentNews aesthetic retune shipped — second UI redesign on top of the Linear/Vercel/Stripe baseline. Anchored on `https://biks2013.github.io/AgentNews/` per a colleague's published reference site. Re-tuned the three-tier token system to AgentNews's 14-token palette (slate + teal accent, IBM Plex Sans/Mono + Newsreader serif), flipped the default theme to light to match AgentNews, added `site/src/styles/agentnews-layout.css` (560 lines of `.hero` / `.section` / `.feature` / `.card` / `.grid-3` / `.tag` / `.eyebrow` / `.theme-toggle` class APIs), and rewrote the 9 simpler marketing surfaces (home, news, skills, tips, glossary, reference, contribute, week-1, day-1) to use the new class API. Heavy data surfaces (my-pins, submit-skill) inherit the new aesthetic through their primitive consumers — visual reskin only, behaviour unchanged. News surface now renders three named sections (AI-News, Deep Dives, Articles) via a render-time discriminator in the new `site/src/lib/news-sections.ts` helper — schema unchanged. AC36/AC37 portability gates verified clean. 215 → 237 site tests pass. See `docs/design/plan-005-agentnews-aesthetic.md`, `docs/reference/investigation-agentnews-aesthetic.md`, project-design.md §S.13.16.) Prior 2026-05-21 (Reddit-rules pivot: in-session attempt at the engagement-floor + JSON-endpoint pipeline hit 403 from GH Actions on Reddit JSON regardless of subdomain. Pivoted to Reddit Application-Only OAuth, then parked when operator couldn't create the Reddit script app (captcha failure across browsers). Current state: Reddit feeds revert to `.rss` + `type: "rss"`; OAuth + engagement-filter + JSON-parser code stays in the repo as dormant ready-to-reactivate scaffolding. Cron shift to 22:00 UTC **stays** — independent of the Reddit-feed mechanism. HN / Wired / Verge unchanged. New required `type: "rss" | "reddit-json"` discriminator in `config/rss-sources.json` stays. Pipeline test count 133 → 205. See DECISIONS 2026-05-21 (three entries — engagement floor, OAuth pivot, OAuth parked).) Prior 2026-05-19 (docs-drift hook shipped — `scripts/sync-doc-counts.mjs` + `.github/workflows/docs-drift.yml` keep AUTO blocks honest; SCOPE.md "Content at a glance" + CLAUDE.md "Content counts" are now the canonical count source. Same day: Content port from team Onboarding guide shipped — 32 entries (10 new glossary terms 5→15, 12 tips 0→12, 9 skills 0→9 cataloguing `556LowCodeNoCode/Skills`, Day-1 journey rewritten with 6 full steps). Variant C auto-promote shipped (high-confidence professional-source items bypass editorial review). Personalization + community contributions shipped (PAT-paste sign-in, unlisted-gist favourites, /my-pins/, /submit-skill/, CI validator). Hub plugin shipped (eleven `/hub-*` commands in `plugin/`). UI redesign shipped — Option 1 hybrid (Linear/Vercel/Stripe aesthetic) — three-tier token system (~245 declarations), 16 portable primitives, MarketingShell wrapping 11 splash-template marketing surfaces, content-page theme override propagating tokens into Pagefind via `--sl-color-*` aliases. See docs/design/plan-004-ui-redesign.md and project-design.md §S.13.

## Vision

A curated Claude Code knowledge hub for bank colleagues — a one-stop shop combining skills catalog, tips & tricks, curated news, onboarding journeys, and a shared vocabulary. Tone: *"what I wish I knew a year ago."* The hub compresses newcomers' time-to-confidence, leveraging the ~6 months of head start the AI unit has over the rest of the organization on Claude Code.

## Audience

- **Primary:** Bank colleagues new to Claude Code (joining over the next 6–12 months)
- **Secondary:** Existing intermediate users hunting for tips, news, or new skills

## Repo & hosting

- **Repo:** `github.com/chomovazuzana/NbgAiHub` (personal account, bootstrap mode)
- **Visibility:** **private** (per user override; supersedes prior public decision — see DECISIONS.md)
- **Hosting:** **RESOLVED 2026-05-25** — GitHub Pages under chomovazuzana at `https://chomovazuzana.github.io/NbgAiHub/`. `site` + `base: '/NbgAiHub'` configured in `site/astro.config.mjs`; postbuild rewrite at `site/scripts/rewrite-base-paths.mjs` prefixes the nine top-level routes in built HTML; Pages workflow at `.github/workflows/deploy-pages.yml`. **Pending operator step:** flip repo to public (free Pages requires it) OR upgrade to Pro for private Pages. See DECISIONS 2026-05-25 "Publish site to GitHub Pages".
- **Content rule:** since repo is private, team-internal content is permissible — but the repo lives under a *personal* account, not bank-managed infrastructure. Bank-confidential material should still go through compliance review before being stored here.
- **Migration path:** transfer to team org and switch to GitHub Team if the project graduates from personal bootstrap.

## Content at a glance

Canonical counts — regenerated by `node scripts/sync-doc-counts.mjs`. Do not edit between the markers; the docs-drift CI job will fail if these go stale.

<!-- AUTO:counts -->
| Pillar | Files |
|---|---|
| Glossary | 34 |
| Tips | 12 |
| Skills | 9 |
| Journeys | 2 |
| News (published) | 54 |
<!-- /AUTO:counts -->

Test counts (regenerated by `node scripts/sync-doc-counts.mjs --with-tests`):

<!-- AUTO:tests -->
| Workspace | Tests passing |
|---|---|
| pipeline/ | 205/205 |
| site/ | — |
| plugin/ | — |
<!-- /AUTO:tests -->

## The hub at a glance

**Five user-facing pillars** (each consumed differently; all share one content shape):

| # | Pillar | What it offers | Consumption pattern | Status |
|---|---|---|---|---|
| 1 | **Skills catalog** | Discovery layer over installable plugins (internal + external) | Browse → install via plugin marketplace | **✅ 9 entries cataloguing `556LowCodeNoCode/Skills` marketplace** (team, claudemd, deploy, uat-panel, frontend-design, database-schema-designer, commit-work, jira, gsd) |
| 2 | **Tips & Tricks** | Patterns, prompts, gotchas, workflow recipes | Read & apply manually | **✅ 12 entries** — prompting fundamentals, survival keys, context discipline, compliance |
| 3 | **News** | Curated tech news, AI-triaged from RSS feeds at build-time | Skim weekly | **✅ pipeline operational; site reads `/news/published/` via glob loader** |
| 4 | **Curated journeys** | Day 1, Week 1, by-role onboarding paths | Follow step-by-step | **✅ Day-1 full 6 steps** (install → first session → survival keys → CLAUDE.md → skills marketplace → tomorrow); Week-1 + by-role still TBD |
| 5 | **Glossary + Reference** | Term definitions (hybrid page + anchor links) + cheatsheet | Lookup as needed | **✅ 15 glossary terms** (original 5 + claude-code, context-window, build-time-vs-runtime, gsd, gh, repository, commit, branch, pull-request, issue); reference page placeholder |

**Cross-cutting substrate** (the same plumbing serves all five pillars):

- **GitHub repo as CMS** — markdown + frontmatter, PR workflow for everything ✅
- **Astro Starlight web UI** — **✅ built; Astro 6.3.5 + Starlight 0.39.2; 10 pages render; serving at `localhost:4321`; production hosting deferred**
- **Hub-as-skill plugin** — **✅ shipped 2026-05-19**; one command (`/plugin marketplace add chomovazuzana/NbgAiHub`) installs the hub into Claude Code; eleven `/hub-*` commands; bundled markdown snapshot; per-user state at `${CLAUDE_PLUGIN_DATA}/state.json`
- **Shared content shape** — 13-key frontmatter schema for news (added `editor_confidence`); 10-key base for skills/tips/glossary/journeys ✅
- **AI strategy** — build-time RSS triage via Azure OpenAI; runtime AI lives in the user's Claude session via the skill; **no AI on the website**
- **Complementary to the Onboarding guide** at `556lowcodenocode.github.io/Onboarding` — the hub deep-links into it, does not duplicate it

## MVP scope — IN

| Item | Status |
|---|---|
| **One curated Day 1 journey** — 6-step path: install → first session → survival keys (incl. `Esc Esc`) → CLAUDE.md (global + project) → skills & team marketplace → where to go next | **✅ SHIPPED 2026-05-19** — `journeys/day-1.md` populated with full 6-step walkthrough; `site/src/pages/start-here/day-1.astro` rewritten to render the journey collection entry via `getEntry('journeys', 'day-1')`; same content also feeds `/hub-onboard day-1` |
| **~10 Tips & Tricks entries** | **✅ 12 SHIPPED 2026-05-19** — prompting fundamentals (6 rules + bad-vs-good openers), correction phrasing, survival keys (Esc), context discipline (`/compact` `/clear`), review-before-accept, Azure-OpenAI compliance |
| **~5 Skills catalog entries** — internal + external, description + install link | **✅ 9 SHIPPED 2026-05-19** — team, claudemd, deploy, uat-panel, frontend-design, database-schema-designer, commit-work, jira, gsd (all cataloguing `556LowCodeNoCode/Skills` marketplace) |
| **~10 Glossary terms** — CLAUDE.md, MCP, skill, plugin, agent, hook, GSD, build-time vs runtime, etc. | **✅ 15 SHIPPED** — original 5 (claudemd, mcp, skill, plugin, agent) + 10 added 2026-05-19 (claude-code, context-window, build-time-vs-runtime, gsd, gh, repository, commit, branch, pull-request, issue) |
| **RSS curation pipeline** — daily GH Action: fetch feeds → Azure OpenAI triage → `/news/incoming` → PR → editorial review → promote to `/news/published` | **✅ BUILT & OPERATIONAL** — 93/93 tests pass (after triage tightening); live runs producing daily PRs. Spec: `docs/refined-requests/rss-pipeline.md`. Verification: `docs/reference/integration-verification-rss-pipeline.md`. **+ unconditional auto-promote (2026-05-19, reverses earlier variant C the same day)** — every relevant triaged item writes directly to `news/published/`; the workflow pushes straight to `main`, no editorial PR. All feeds carry `auto_promote_eligible: true`; the field is retained as a per-feed kill switch but is currently unused. Cross-feed title dedup is now the load-bearing quality control. Workflow still branches on `mode` (`auto_only` / `mixed` / `review_only` / `empty`) so flipping one feed back to `false` is a one-line config change. **+ rolling 7-day retention** — same run prunes any `news/published/<YYYY-MM-DD>-*.md` strictly older than `today - 7d`; deletions land in the same commit as the day's new items. Both DECISIONS 2026-05-19. |
| **Astro Starlight static site** with beginner/advanced filter | **✅ BUILT** — Astro 6.3.5 + Starlight 0.39.2, 10 pages (Home, Start Here→Day 1/Week 1, News, Skills, Tips, Glossary, Reference, Contribute, 404), 7 components (HomeHero, NewsPanel, NewsList, AudienceBadge, SkillCard, AudienceFilter + bonus ConfidenceChip), Pagefind search, dark theme default. Spec: `docs/refined-requests/astro-starlight-site.md`. Verification: `docs/reference/integration-verification-astro-site.md`. Hosting open. |
| **Hub-as-skill plugin** — eleven `/hub-*` commands (`/hub`, `/hub-search`, `/hub-news`, `/hub-tips`, `/hub-skills`, `/hub-glossary`, `/hub-onboard <journey>`, `/hub-install <skill-id>`, `/hub-audience <b|a|both>`, `/hub-refresh`, `/hub-open [section] [subsection]`) | **✅ BUILT & OPERATIONAL (2026-05-19)** — TypeScript workspace at `plugin/` sibling to `pipeline/` and `site/`. **130/130 tests pass** (12 lib test files + manifest + end-to-end entry-script smoke), typecheck + lint clean, esbuild produces 11 ESM entries in `dist/`, bundled snapshot of repo content via `npm run build:snapshot`. Marketplace at repo-root `.claude-plugin/marketplace.json`, plugin manifest at `plugin/.claude-plugin/plugin.json`. Spec: `docs/refined-requests/hub-plugin.md`. Plan: `docs/design/plan-003-hub-plugin.md`. Verification: `docs/reference/integration-verification-hub-plugin.md`. |
| **Hybrid glossary** — canonical `/glossary` page + inline anchor links | **✅ page renders with 5 anchored terms; pattern proven** |
| **Public/private gating** — `internal: true|false` frontmatter | schema defined; in use by pipeline + site |
| **Per-user favourites (PAT-paste + unlisted-gist-backed)** — sign in by pasting a `gist`-scope PAT; pin any content item; favourites stored in the user's own unlisted GitHub gist `nbgaihub-favorites.json`; `/my-pins/` page renders pinned items; same gist consumable by future Claude-side `/hub-*` skill | **✅ BUILT (2026-05-19)** in commits `c1df291`, `5a08260`, `64f83b2`. 127/127 site tests pass. See `docs/refined-requests/personalization-and-contributions.md`, plan `docs/design/plan-003-personalization-and-contributions.md`, gist contract `docs/reference/gist-contract.md`. |
| **Skill submission web form (URL-redirect to GitHub editor)** — `/submit-skill/` page; anonymous-accessible; live-validated against the 17-rule frontmatter contract; serialises markdown and redirects to GitHub's `new/main/skills?filename=&value=` editor (7000-char clipboard fallback for oversize payloads); CI validator workflow on `skills/**/*.md` PRs surfaces `::error` annotations | **✅ BUILT (2026-05-19)** in commits `c1df291`, `5a08260`, `64f83b2`. Validator at `pipeline/src/validators/skill.ts` (17 rules); workflow at `.github/workflows/validate-skill-submission.yml`. See auto-synced test-count table for current pipeline test total. |

## Deferred — LATER

- Week 1 / by-role curated journeys (backend dev, data scientist, ML engineer, etc.)
- Full-text or semantic search across content (currently Pagefind covers full-text; semantic deferred)
- Greek-language content
- Authentication / SSO for gated content
- Analytics on what newcomers click
- Expanded news sources beyond the initial five
- Hero image extraction for news items (RSS thumbnail + og:image fallback) — `hero_image` field reserved in site schema (optional) for forward compat
- War stories / post-mortem pillar (6th pillar candidate)
- Migration to team org if/when bank-specific gated content becomes needed
- Site production hosting (Pro Pages / Vercel / Netlify / Cloudflare)
- Cosmetic refactor of Zod 4 deprecations in `site/src/content.config.ts` (`z.string().url()` → `z.url()`)

## Out of scope — NO

- Live chat or forum
- Hosting user-generated content
- Marketing-style branding
- Live chatbot widget on the website (the Claude skill IS the chatbot)
- Client-side embeddings or semantic search backend
- Bank-confidential content in this repo (structural constraint of personal account)

## Open questions

For full RSS pipeline context, see refined request: `docs/refined-requests/rss-pipeline.md`. For the site, see `docs/refined-requests/astro-starlight-site.md`.

- **Hosting:** GitHub Pages via Pro, Vercel/Netlify/Cloudflare free tier, or defer hosting until MVP content exists?
- **Proof-of-life user:** which specific newcomer joining in the next 4–8 weeks anchors the MVP deadline?
- **Skill distribution:** standalone marketplace at `chomovazuzana/NbgAiHub` or also list in `556LowCodeNoCode/Skills`?
- **RSS source list — live in production with 5 feeds, two source groups** (revisable in `config/rss-sources.json` without code change). Source-group routing in `pipeline/src/triage.ts` SYSTEM_PROMPT:
  - **Reddit group** (`type: "rss"` — reverted from `reddit-json` per DECISIONS 2026-05-21 third entry; engagement filter scaffolding lives in the repo ready to reactivate if OAuth becomes available later):
    1. r/ClaudeAI — `https://www.reddit.com/r/ClaudeAI/.rss`
    2. r/ClaudeCode — `https://www.reddit.com/r/ClaudeCode/.rss`
  - **Major tech/AI news group** (`type: "rss"`; professional news + breakthrough AI only; no engagement floor — these are editorial sources):
    3. Hacker News frontpage — `https://hnrss.org/frontpage` *(unfiltered; replaced earlier Claude-keyword variant on 2026-05-18)*
    4. Wired AI — `https://www.wired.com/feed/tag/ai/latest/rss`
    5. The Verge — `https://www.theverge.com/rss/index.xml` *(full firehose; LLM filters the non-AI noise)*
  - **Dropped feeds** (2026-05-18, by user direction): Anthropic news (RSS feed deleted by Anthropic — returned 404), Claude Code GitHub releases (`releases.atom`), Simon Willison's blog. The latter two are easy to re-add if signal loss is felt.
- **Editorial cadence:** daily Action at **22:00 UTC = 00:00 Europe/Athens (winter) / 01:00 Athens (DST)** + ad-hoc PR review (current). Shifted from 05:00 UTC on 2026-05-21 so the previous day's Reddit posts have time to accumulate `score` / `num_comments` and clear the engagement floor. Weekly summary PR or twice-weekly cron remain open alternatives. Cron lives at `.github/workflows/rss-triage.yml` line 8 — one-line YAML change to flip.
- **News storage model:** per-item permanent (current — files accumulate forever) vs rolling N-day window vs hybrid (storage permanent + UI filters)? Currently per-item permanent; site reads via glob loader and renders chronologically.
- **News item hero image:** add `hero_image` frontmatter field, extracted from RSS thumbnail + og:image fallback? Deferred but small to implement. Site schema already accepts `hero_image` as optional (forward compat).

## Demo-ability checklist (manager review)

- [x] **Site renders locally** — `cd site && npm run dev` → `http://localhost:4321` returns 200; homepage shows hero + tagline; all 9 sidebar entries clickable
- [x] **Day 1 journey page populated with all 6 steps + deep-links into the Onboarding guide** *(2026-05-19 — `journeys/day-1.md` + `site/src/pages/start-here/day-1.astro` renders from the collection)*
- [x] **At least 1 skill entry, 1 tip visible** *(2026-05-19 — 9 skills + 12 tips shipped)*
- [x] **At least 5 glossary terms visible** *(15/10 seeded — 2026-05-19)*
- [x] At least 1 news item visible *(43 items in PR #1 on 2026-05-18; need PR merge to surface in `news/published/`)*
- [x] **Beginner/Advanced/Both filter wired** *(AudienceFilter component built, `localStorage.nbgaihub.audience` persistence; visible no-op until news populated)*
- [x] **`/hub` commands work from a fresh Claude Code install** *(eleven commands ship in `plugin/`; install via `/plugin marketplace add chomovazuzana/NbgAiHub`; smoke-tested end-to-end on 2026-05-19)*
- [x] **One full end-to-end RSS pipeline run completed** *(run `26047997638`, 2m46s, PR #1, then PR #2/#3 with tightened triage)*
- [x] **Hub installable as a plugin (`/plugin marketplace add chomovazuzana/NbgAiHub`) in one command** *(marketplace.json points at `./plugin`; plugin.json declares `nbg-ai-hub`)*
- [x] **SCOPE.md + DECISIONS.md tell the story of how we got here** *(this file + 20+ DECISIONS entries through 2026-05-18)*
- [x] **Signed-in user can pin and see pins on `/my-pins/`** *(verified in build — PAT-paste flow validates against `GET /user`; first pin lazily creates unlisted gist `nbgaihub-favorites.json`; `/my-pins/` joins gist `favourites[]` against build-time `<type>-index.json`; stale references render as dimmed "no longer available — unpin" rows)*
- [x] **Anonymous visitor can submit a skill via `/submit-skill/` and reach GitHub's editor with the content pre-filled** *(verified in build — form is anonymous-accessible; live frontmatter validation mirrors the 17 CI rules; submit serialises YAML + body, URL-encodes, and either redirects directly (≤7000 chars) or copies content to clipboard + redirects to bare new-file URL (>7000 chars); CI validator workflow re-runs the same rules on the PR)*
- [x] **UI redesign shipped — Apple-influenced Linear/Vercel/Stripe aesthetic** *(2026-05-19 — see docs/reference/integration-verification-ui-redesign.md for the 39/39 AC verification report; user evaluation at localhost:4321 is the final gate before commit/escalation decision)*
