---
description: Print the marketplace install command for a hub-listed skill
argument-hint: <skill-id>
---

Present the install command below verbatim. Suggest the user run the displayed command line in their Claude Code session to install the skill. Do not modify the command — the maintainer authored it exactly as written.

!`node "${CLAUDE_PLUGIN_ROOT}/dist/hub-install.mjs" $ARGUMENTS`
