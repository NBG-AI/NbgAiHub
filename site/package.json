{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "tsx scripts/build-pin-index.ts && astro check && astro build && node scripts/rewrite-base-paths.mjs",
    "preview": "astro preview",
    "check": "astro sync && astro check",
    "typecheck": "astro check",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "@astrojs/starlight": "^0.39.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "gray-matter": "^4.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.6",
    "yaml": "^2.8.3"
  },
  "overrides": {
    "yaml-language-server": {
      "yaml": "^2.8.3"
    }
  }
}
